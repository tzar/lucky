class <%= pluralized_resource %>::Show < BrowserAction
  needs <%= underscored_resource %> : <%= resource %>
  quick_def page_title, "Show"

  def content
    h1 "The record"
    # TODO: List all the columns
  end
end
